version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/easylink-solar/web

hooks:
  AfterInstall:
    # No runas: runs as root — fix ownership after CodeDeploy copies files as root.
    - location: scripts/chown-web.sh
      timeout: 120
    - location: scripts/install.sh
      timeout: 120
      runas: ubuntu

  ApplicationStart:
    - location: scripts/start.sh
      timeout: 600
      runas: ubuntu
